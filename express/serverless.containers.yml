namespace: scf

containers:
  service-express:
    compute:
      src: ./service
      type: awsLambda
      environment:
        SERVICE_NAME: service-express

    routing:
      pathPattern: /express*
