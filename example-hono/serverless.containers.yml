namespace: scf-example-hono

provider:
  type: aws
  aws:
    region: us-east-1

containers:
  service-hono:
    src: ./service
    routing:
      pathPattern: /*
      pathHealthCheck: /health
    compute:
      type: awsFargateEcs
      environment:
        SERVICE_NAME: service-hono
