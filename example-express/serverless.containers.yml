namespace: scf-example-express

provider:
  type: aws
  aws:
    region: us-east-1

containers:
  service-express:
    src: ./service
    routing:
      pathPattern: /*
      pathHealthCheck: /health
    compute:
      type: awsLambda
      environment:
        SERVICE_NAME: service-express
      # awsIam:
      #   customPolicy:
      #     Version: "2012-10-17"
      #     Statement:
      #       - Effect: Allow
      #         Action:
      #           - dynamodb:GetItem
      #           - dynamodb:PutItem
      #           - dynamodb:UpdateItem
      #           - dynamodb:DeleteItem
      #           - dynamodb:Query
      #           - dynamodb:Scan
      #           - dynamodb:BatchGetItem
      #           - dynamodb:BatchWriteItem
      #         Resource:
      #           - "*"
